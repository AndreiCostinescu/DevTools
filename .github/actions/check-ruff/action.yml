name: Check ruff
description: Fails if any tracked Python file is not formatted or fails linting per the repo's ruff.toml.

runs:
  using: composite
  steps:
    - name: Check formatting (ruff format)
      shell: bash
      run: ruff format --check .

    - name: Check linting (ruff check)
      shell: bash
      run: ruff check .
